{
  "name": "rate-my-openapi",
  "version": "1.3.0",
  "scripts": {
    "build": "tsc --build",
    "format": "prettier --write .",
    "release": "npm version minor && git add . && git commit -m 'release' && git push && git push --tags"
  },
  "workspaces": [
    "apps/api",
    "packages/*"
  ],
  "devDependencies": {
    "@tsconfig/node18": "^2.0.1",
    "prettier": "^2.8.8",
    "typescript": "^5.1.3"
  }
}
